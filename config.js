/* =============================================================================
   config.js — the ONE file you edit when the backend moves.
   -----------------------------------------------------------------------------
   Load this before app.js and driver.js. Both read the value set here.
   ============================================================================= */

// Paste your Railway URL here. No slash at the end.
// Example: "https://ngubohlanga-production.up.railway.app"
//
// Leave it as an empty string ONLY if the backend is serving these pages itself
// (one Railway service, no GitHub Pages).
window.NGUBO_API = "https://ngubohlanga-production.up.railway.app";
